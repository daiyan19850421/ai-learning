A. 项目与职责（决定你能卖什么级别）
A1. 两个项目里，你分别是 Owner / 核心开发 / 参与？各约占多少比例？
两个项目全部是核心研发，都是 100%

A2. 「智慧融合指挥调度系统」用一句话说清：谁在用、解决什么问题。你负责的最核心 1～2 个模块 是什么？
高速、钻井平台、应急指挥中心等部门，用来实现音、视频通话为主的调度工作。
我负责的核心模块有：通过java后台调用freeswitch实现音视频通话和调度类功能（拨号计划）、设备管理、用户管理等大部分模块

A3. 指挥调度里，你遇到过 最难的一个生产或联调问题 是什么？（现象 → 你怎么查 → 最后怎么改的 → 结果）
首页手动同步摄像头列表功能响应非常慢 → 首先查前端调试台显示发现没有报错，后台代码逐条判断增删改，手动调用摄像头服务器得到列表容量很大
→ 第三方平台不能改，请求列表的时间不能省，Redis 中记录当前使用 A 表查询列表，点击同步后直接清除并插入到 B 表，Redis 中记录当前使用 B → 结果只剩请求第三方接口和插入输入的几秒

A4. 「阳光金融客户洞察系统」里，客户洞察 具体指什么（标签、报表、推荐、风控…）？你负责哪一块？
标签表的设计、数据统计、查询性能优化


A5. 该金融项目里，最大的表 大概多少行？有没有做过 慢 SQL / 索引优化？有的话举一个例子；没有就写「没有」。
最大的表有十几亿行。例如用户标签统计结果的分析，由于维度过于复杂导致正常查询都不能完成，最后按照不同统计维度做了定时任务，从新表中直接查询结果


B. Java（10 年岗必问）
B1. @Transactional 在 同类内自调用 时为什么常常不生效？你们项目里怎么规避的？
不清楚


B2. 线程池（或异步任务）在你们系统里怎么用的？若调度系统要 同时处理上千路状态上报，你会从哪几方面考虑（不必全对，说你的真实思路）？
不清楚


B3. 说一个你在项目里用过的 并发工具或模式（如 ConcurrentHashMap、锁、MQ 消峰等），为什么用它。
没用过

C. MySQL
C1. 联合索引 (a, b, c)，下面哪些能较好用到索引（写能/不能/部分能）？
WHERE a=1 AND b=2 能
WHERE b=2 AND c=3 不确定，印象能
WHERE a=1 ORDER BY b 估计不能

C2. 慢查询排查，你 实际会做的前 3 步 是什么？（按顺序）
判断代码是否有问题
SQL 中是否有效利用索引
查看执行计划

D. 系统设计（看是否够「资深/架构」）
D1. 指挥调度里若要做 「任务下发 + 各节点 ACK + 超时重试」，你会怎么拆模块、用什么存储状态？（开放题，说你的设计即可）
不清楚

